{
  "_why": [
    "Milestone 0.4. rule-diff.sh reports every per-rule count change between the",
    "last release and this build. Reporting is not accountability: a drop and a",
    "correct narrowing look identical in that output, and the only thing telling",
    "them apart was whoever happened to read the job summary.",
    "",
    "This file is the other half. Every rule whose count DROPS must have an entry",
    "here saying why. An unexplained drop fails the job; so does an entry that no",
    "longer describes a real drop, because a ledger nothing validates rots into",
    "decoration.",
    "",
    "The baseline is the last released nox, so entries accumulate between releases",
    "and are cleared when one is cut. `nox_release` records which release they are",
    "written against: if it does not match the tag rule-diff resolved, the ledger",
    "is stale as a whole and says so rather than silently passing."
  ],
  "nox_release": "v1.35.0",
  "classifications": {
    "duplicate-retired": "a second rule reported the same condition; one ID was retired into the other, which carries the alias",
    "applicability-narrowed": "the rule does not apply to this subject or resource kind, and now says so",
    "sanitizer-recognized": "the value reaching the sink is neutralized for this vulnerability class",
    "refined-away": "a cheaper semantic fact refutes the candidate the pattern generated",
    "rule-removed": "the rule was deleted outright; no survivor reports the condition",
    "context-narrowed": "the rule's context requirement is now measured in characters as well as lines, so a keyword far away on the same physical line no longer qualifies as context"
  },
  "entries": [
    {
      "rule": "SEC-696",
      "classification": "duplicate-retired",
      "pr": 641,
      "reason": "SEC-696's pattern is `[a-zA-Z0-9]{32}` with the keyword \"timber\", and its context requirement was measured in LINES (±4). reitit's dev-resources/json/json100k.json is minified JSON: one line of 101,941 characters. It contains the string \"2114 timber wolf trail\" — a fake street address in generated test data — and being on the same physical line as everything else in the file, that one word authorised the rule for every 32-character run on it: 421 HIGH-severity findings from one line of fixture JSON. Proximity is now bounded at 512 characters, which is generous for `timber_api_key = \"...\"` and three orders of magnitude short of a blob. The 3 remaining findings are matches that really do sit near the keyword. No corpus lost an expectation: all 20 precision corpora and the refutation suite are unchanged at 231/0/0 and 37/0/0, and the four real-repo taint controls (ring, certbot, shelljs, plug) are unchanged. A SECOND, later drop takes the remaining 3 (#638). All of them are in the same file: one md5 value, the first 32 characters of a sha256, and the second 32 characters of the same sha256 — digests in generated test data, not Timber API keys. The digest-label refutation added for SEC-163 covers them, including the second half, because the label belongs to the whole hex run and a window a matcher cut out of it inherits `\"sha256\":`. Every line was read. SEC-696 now has no finding anywhere in the corpus, so the gate witnesses it in neither direction and its behaviour rests on the analyzer tests alone. The rule is now retired outright into SEC-005 (#641), and the measurement that decided it is sharper than the count. On the shape SEC-696 exists for — `timber_api_key = \"<32 alphanumerics>\"` — it never reached the output at all: SEC-005 wins the dedup contest and reports it. On the shape it gets wrong — a 32-character run anywhere near the word \"timber\" in prose — it was the ONLY rule that fired. Suppressed exactly where it was right and the sole reporter exactly where it was wrong is not a threshold needing tuning; it is a rule with no unique correct output. It was one of 69 imported from Gitleaks with the shape `[a-zA-Z0-9]{32}` plus one vendor keyword, and the only one of the 69 whose keyword is an ordinary English word AND which carries no secret-shape post-filter. SEC-005 holds the alias with the frozen pattern."
    },
    {
      "rule": "IAC-246",
      "classification": "applicability-narrowed",
      "pr": 636,
      "reason": "The Serverless Framework family scoped itself with `*.yml`/`*.yaml` alongside the specific names, which made the specific names decoration: every rule in the family applied to every YAML file in any repository. Applicability is now decided by the document — a Serverless rule fires only where the document declares `service`/`provider` — so the filename is a hint about what to parse rather than evidence that the rule applies. Every dropped finding was read: 2 findings on ECS/*/clusters/private-vpc.yaml in aws-cloudformation-templates — CloudFormation templates, not Serverless manifests. No affected file went from flagged to clean: the Kubernetes Secret manifests keep SEC-254/IAC-351/IAC-356 and chronos's workflow keeps IAC-013/IAC-309/SEC-073, so what was removed is the wrong framework's rule reporting on a smell that the right rules still report."
    },
    {
      "rule": "IAC-247",
      "classification": "applicability-narrowed",
      "pr": 636,
      "reason": "The Serverless Framework family scoped itself with `*.yml`/`*.yaml` alongside the specific names, which made the specific names decoration: every rule in the family applied to every YAML file in any repository. Applicability is now decided by the document — a Serverless rule fires only where the document declares `service`/`provider` — so the filename is a hint about what to parse rather than evidence that the rule applies. Every dropped finding was read: 63 findings on AWSSupplyChain/SapPrivateLink/*.yaml in aws-cloudformation-templates — CloudFormation templates, not Serverless manifests. The largest single drop, and every one of them a CFN IAM policy scored against the wrong framework's rule. No affected file went from flagged to clean: the Kubernetes Secret manifests keep SEC-254/IAC-351/IAC-356 and chronos's workflow keeps IAC-013/IAC-309/SEC-073, so what was removed is the wrong framework's rule reporting on a smell that the right rules still report."
    },
    {
      "rule": "IAC-248",
      "classification": "applicability-narrowed",
      "pr": 636,
      "reason": "The Serverless Framework family scoped itself with `*.yml`/`*.yaml` alongside the specific names, which made the specific names decoration: every rule in the family applied to every YAML file in any repository. Applicability is now decided by the document — a Serverless rule fires only where the document declares `service`/`provider` — so the filename is a hint about what to parse rather than evidence that the rule applies. Every dropped finding was read: 2 findings on AWSSupplyChain/SapPrivateLink/*.yaml — CloudFormation, not Serverless. No affected file went from flagged to clean: the Kubernetes Secret manifests keep SEC-254/IAC-351/IAC-356 and chronos's workflow keeps IAC-013/IAC-309/SEC-073, so what was removed is the wrong framework's rule reporting on a smell that the right rules still report."
    },
    {
      "rule": "IAC-254",
      "classification": "applicability-narrowed",
      "pr": 636,
      "reason": "The Serverless Framework family scoped itself with `*.yml`/`*.yaml` alongside the specific names, which made the specific names decoration: every rule in the family applied to every YAML file in any repository. Applicability is now decided by the document — a Serverless rule fires only where the document declares `service`/`provider` — so the filename is a hint about what to parse rather than evidence that the rule applies. Every dropped finding was read: 21 findings across three repositories: 7 on CloudFormation templates, 12 on Kubernetes Secret manifests in kubernetes/examples, and 2 on chronos's .github/workflows/ci.yml. None is a Serverless manifest. Measured separately on MacPaw/OpenAI@0.5.1, this rule fired 268 times at CRITICAL on openapi.yaml against Ruby documentation samples reading `api_key: \"My API Key\"`, advising ${ssm:/path/to/secret} for a file with no such concept. No affected file went from flagged to clean: the Kubernetes Secret manifests keep SEC-254/IAC-351/IAC-356 and chronos's workflow keeps IAC-013/IAC-309/SEC-073, so what was removed is the wrong framework's rule reporting on a smell that the right rules still report. Adding geerlingguy/ansible-for-devops witnesses 8 more, all read, and this is the one entry where something is lost. 6 of the 8 are `password: \"1234\"` / `password: secret` in Ansible vars and tasks, and IAC-225 and IAC-351 keep reporting them. The other 2 are `MYSQL_ROOT_PASSWORD: root` and `MYSQL_PASSWORD: flask` in a docker_container env block, and nothing reports those: IAC-225 requires the value to be QUOTED (`password\\s*:\\s*['\\\"]`) and IAC-351 is scoped to CI files. That is a pre-existing hole this repository exposes rather than one this change opened -- a Serverless rule was the only thing covering it -- and it is recorded here so the next reader finds it stated rather than has to rediscover it."
    },
    {
      "rule": "IAC-257",
      "classification": "applicability-narrowed",
      "pr": 636,
      "reason": "The Serverless Framework family scoped itself with `*.yml`/`*.yaml` alongside the specific names, which made the specific names decoration: every rule in the family applied to every YAML file in any repository. Applicability is now decided by the document — a Serverless rule fires only where the document declares `service`/`provider` — so the filename is a hint about what to parse rather than evidence that the rule applies. Every dropped finding was read: 4 findings on Config/Config.yaml and ElastiCache/Elasticache-snapshot.yaml — CloudFormation, not Serverless. No affected file went from flagged to clean: the Kubernetes Secret manifests keep SEC-254/IAC-351/IAC-356 and chronos's workflow keeps IAC-013/IAC-309/SEC-073, so what was removed is the wrong framework's rule reporting on a smell that the right rules still report."
    },
    {
      "rule": "IAC-231",
      "classification": "applicability-narrowed",
      "pr": 637,
      "reason": "The Kustomize family scoped itself with `*.yaml`/`*.yml` alongside kustomization.yaml, which made the specific names decoration: every rule in the family applied to every YAML file in any repository — the identical shape fixed for the Serverless family in #636. Applicability is now decided by the document: a Kustomize rule fires only where the document declares `kind: Kustomization`, a kustomize.config.k8s.io apiVersion, a kustomization-only field, or a `resources:` LIST (as against the `resources:` MAPPING a Helm values file or a pod spec declares). Measured across the whole corpus, 37 of the family's 40 findings were on documents that are not kustomizations; the 3 that are — IAC-242 on podinfo's dev/staging/production overlays — are unchanged. Every dropped finding was read: 5 across two repositories. 3 on Kubernetes manifests in kubernetes/examples (mysql-pod.yaml, and minio's statefulset and deployment) and 2 on chronos's test/integration/docker-compose.yml. None is a kustomization. The Kubernetes smell is not lost: IAC-031 (\"Container image uses latest tag in Kubernetes manifest\") now reports all 3 at exactly the same lines — it could not before, because it was anchored with `$` and no `(?m)` and so fired only on a file's last line, fixed in the preceding commit. One file went from flagged to clean: chronos's test/integration/docker-compose.yml, whose only two findings were this rule on `image: ${CHRONOS_IMAGE:-ghcr.io/...:latest}`. No rule covers a latest tag in a compose file, and that gap is real, but a Kustomize rule firing on a docker-compose service was never its answer. Adding geerlingguy/ansible-for-devops to the corpus witnesses 5 more, all read: `image: data:latest`, `flask:latest`, `db:latest` and `test:latest` in Ansible docker_container tasks, plus `${MOLECULE_DISTRO:-rockylinux9}-ansible:latest` in a molecule config. IAC-031 picks up the first four at exactly the same lines. The fifth is not covered by anything: no rule resolves a `${VAR:-default}` substitution before reading the tag, and that gap is named here rather than left to the Kustomize rule. The docker-compose gap named above is now closed by IAC-501 (#642), which resolves `${VAR:-default}` the way Compose does before reading the tag: both chronos findings are reported again, at the same lines, by a rule that says \"Docker Compose service … is not pinned\" and names the reference the default resolves to. The `${MOLECULE_DISTRO:-rockylinux9}-ansible:latest` form in ansible-for-devops is a molecule config rather than a Compose file and stays unreported."
    },
    {
      "rule": "IAC-232",
      "classification": "applicability-narrowed",
      "pr": 637,
      "reason": "The Kustomize family scoped itself with `*.yaml`/`*.yml` alongside kustomization.yaml, which made the specific names decoration: every rule in the family applied to every YAML file in any repository — the identical shape fixed for the Serverless family in #636. Applicability is now decided by the document: a Kustomize rule fires only where the document declares `kind: Kustomization`, a kustomize.config.k8s.io apiVersion, a kustomization-only field, or a `resources:` LIST (as against the `resources:` MAPPING a Helm values file or a pod spec declares). Measured across the whole corpus, 37 of the family's 40 findings were on documents that are not kustomizations; the 3 that are — IAC-242 on podinfo's dev/staging/production overlays — are unchanged. Every dropped finding was read: 9, all on Kubernetes manifests in kubernetes/examples — 5 flexvolume Pods, elasticsearch/rbac.yaml, a GlusterFS Secret, a GlusterFS StorageClass and a StorageOS StorageClass. None is a kustomization, and two of them are not even a deployment namespace: the rule's `namespace:\\s*default` matched `secretNamespace: \"default\"`, the namespace a Heketi credential lives in, and reported it at HIGH confidence as \"Kustomize deploys to default namespace\". No affected file went from flagged to clean, and the smell is not lost: IAC-143 (\"Kubernetes resource deployed to default namespace\") reports all 9 at exactly the same lines, before and after. Adding geerlingguy/ansible-for-devops witnesses 8 more, all read: `namespace: default`, `release_namespace: default` and `ansible_kubectl_namespace: default` in Ansible k8s_module and helm tasks. IAC-143 reports all 8 at exactly the same lines, before and after."
    },
    {
      "rule": "IAC-238",
      "classification": "applicability-narrowed",
      "pr": 637,
      "reason": "The Kustomize family scoped itself with `*.yaml`/`*.yml` alongside kustomization.yaml, which made the specific names decoration: every rule in the family applied to every YAML file in any repository — the identical shape fixed for the Serverless family in #636. Applicability is now decided by the document: a Kustomize rule fires only where the document declares `kind: Kustomization`, a kustomize.config.k8s.io apiVersion, a kustomization-only field, or a `resources:` LIST (as against the `resources:` MAPPING a Helm values file or a pod spec declares). Measured across the whole corpus, 37 of the family's 40 findings were on documents that are not kustomizations; the 3 that are — IAC-242 on podinfo's dev/staging/production overlays — are unchanged. Every dropped finding was read: 23 across two repositories, and every one is a false positive on its face. 21 in aws-cloudformation-templates, where the rule's `count:\\s*1\\b` matched `Count: 1` in a CloudFormation ResourceSignal — the number of success signals the stack waits for — and `InstanceCount: 1` on an EMR master instance group, reporting both as \"Kustomize sets replica count to 1 (no HA)\". 2 in podinfo on `replicaCount: 1` in charts/podinfo/values.yaml and values-prod.yaml, which are Helm values files. No affected file went from flagged to clean. Adding geerlingguy/ansible-for-devops witnesses 1 more: `exact_count: 1` in an AWS EC2 provisioning task, reported as a Kustomize replica count."
    },
    {
      "rule": "SEC-163",
      "classification": "duplicate-retired",
      "pr": 641,
      "reason": "SEC-163 is \"High-entropy hex string detected (possible secret key)\", gated by a secret-suggestive keyword anywhere on the line. Measured across the whole corpus it produced 122 findings and every one was read. 117 were the value of a JSON field named `md5` in two generated fixtures in metosin/reitit — `\"password\":\"buffy\",\"salt\":\"UKfGRyKe\",\"md5\":\"ff252d31f9d6a7e19f2b28521aa1f367\"` — where the sibling `password` field 40 characters away is what authorised them, so the character bound #633 gave SEC-696 would not have removed a single one: the rule was not misjudging distance, it was reporting a password HASH as a possible key. 4 were the gist id in `https://gist.github.com/matthewberryman/fa21ca796c3a2e0dfe8224934b7b055c`, inside a bash script folded into one CloudFormation Fn::Sub scalar. Two refutations now name those cases: a hex value whose own label declares it a digest (md5, sha*, checksum, etag, integrity, …) is a digest, and a hex run in a URL PATH is how that URL names a resource. Both are gated on the value being entirely hexadecimal, so no provider rule can reach them, and the URL check stops at the authority so a credential in userinfo is untouched. The 1 surviving finding is certbot's `challenges.DNS01(token=b\"17817c66b60ce2e4012dfad92657527a\")`, which the label says is a token. Raising the entropy threshold was tried FIRST and refuted by measurement: require_context also grants the -0.5 context discount, so SEC-163's declared 3.5 runs at 3.0, but charging the declared value removed three real Ceph admin keys from kubernetes/examples through SEC-162, the sibling rule on the same matcher. Recall is controlled by new fixtures — the rule had no true-positive expectation anywhere in 19 precision suites — covering the Python, bytes-literal, JSON and HTTP-header idioms. No file went from flagged to clean and no other rule moved; all 19 precision suites and the refutation suite are unchanged. One number needs reconciling with the job summary above it: the 122 is measured against the CURRENT tree, which already carries #633, while this gate's baseline is v1.35.0 and shows reitit at 12 -> 0. Both are right and they are the same event. At release time SEC-696 was firing on those spans and outranking SEC-163 in dedupBySpecificity (secret_shape beats entropy), so it suppressed 105 of them; #633 took SEC-696 from 421 to 3 on that file and handed the spans to the rule below it, which is why SEC-163 rose 12 -> 117 in the interval. That rise was printed by this gate and never questioned, because rises are reported and only drops are gated. The last 1 goes with the rule itself (#641). SEC-163 is retired into SEC-161, which now carries `hex` as a candidate kind under its own threshold and context requirement. It could not simply be deleted into SEC-161's 5.0, because a threshold is meaningful only against its alphabet — Shannon entropy cannot exceed log2(16) = 4.0 bits over hex, so 5.0 rejects every hex string that exists, which is the defect #467 fixed when the rule shipped at 4.5. The entropy matcher therefore learned to hold a policy per kind first. The one surviving finding, certbot/src/certbot/tests/acme_util.py:19 col 34, is reported by SEC-161 at exactly the same position: SEC-161 goes 21 -> 22 in the same repository and nothing else in the corpus moves. SEC-161 carries the alias with SEC-163's frozen `[0-9a-fA-F]{32,}` pattern, and the `scan.secrets.hex_threshold` / `require_context` settings are unchanged for users — they now write SEC-161's per-kind keys, so raising the generic threshold cannot silently push the hex one past its ceiling."
    },
    {
      "rule": "IAC-243",
      "classification": "applicability-narrowed",
      "pr": 639,
      "reason": "IAC-243 is \"Kustomize uses deprecated vars feature\", pattern `vars:\\s*\\n`, scoped by the family's `*.yaml`/`*.yml` catch-all. An Ansible playbook opens a `vars:` block as a matter of course, so the rule reported 23 of them across geerlingguy/ansible-for-devops -- every `vars:` in every playbook and every test -- as a deprecated Kustomize feature. The document-kind gate added in #637 removes them: none of these files declares `kind: Kustomization`, a kustomize.config.k8s.io apiVersion, a kustomization-only field or a `resources:` list. Every dropped line was read; all 23 are the literal token `vars:` opening an Ansible variable block. This drop appears only now because the repo that witnesses it was added in the same change -- no other entry in the corpus contains an Ansible playbook, which is the reason it was added. 11 files went from flagged to clean (collection/main.yml, jenkins/provision.yml, nodejs-role/playbook.yml, test-plugin/main.yml and 7 under tests/); in each the only finding was this one, and none of them is a kustomization."
    },
    {
      "rule": "IAC-214",
      "classification": "duplicate-retired",
      "pr": 640,
      "reason": "IAC-214 (\"Ansible collection without version pin\") and IAC-211 (\"Ansible Galaxy role without version pin\") report one condition split by spelling: a Galaxy requirements entry names its dependency with `src:` OR with `name:`, never both, so each rule was blind to half of the condition. IAC-211 now accepts either and carries the IAC-214 alias with the pattern that rule held at retirement, so a waiver written against IAC-214 keeps matching exactly what it used to cover. Every dropped line was read: 13 in geerlingguy/ansible-for-devops, and 12 of them are reported by IAC-211 at exactly the same line — `- name: geerlingguy.apache`, `- name: community.kubernetes` and ten more, all unpinned entries in a requirements.yml. The 13th, https-self-signed/provisioning/vars/main.yml:19, is `server_hostname: https.test`: the last line of a vars file, matched only because `$` meant end of TEXT and the hostname happens to look like a dotted Galaxy name. That is a false positive and it is gone; the file is the one that went from flagged to clean. IAC-211 rises 0 -> 65 in the same repository, every one an unpinned Galaxy requirement in a requirements.yml — it could not fire at all before, being anchored to end of text."
    }
  ]
}
